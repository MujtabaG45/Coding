name: Remote Devbox SSH

on:
  workflow_dispatch:

jobs:
  start-ssh-tunnel:
    name: Start SSH Tunnel via ngrok
    runs-on: ubuntu-latest

    steps:
      - name: Install OpenSSH Server & GitHub CLI
        run: |
          sudo apt update
          sudo apt install -y openssh-server gh
          sudo sed -i 's/#Port 22/Port 2222/' /etc/ssh/sshd_config
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
          sudo systemctl enable ssh
          sudo systemctl restart ssh

      - name: Add public SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > ~/.ssh/authorized_keys
          chmod 600 ~/.ssh/authorized_keys

      - name: Install ngrok
        run: |
          curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
          echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
          sudo apt update
          sudo apt install -y ngrok
          ngrok config add-authtoken "${{ secrets.NGROK_AUTH_TOKEN }}"

      - name: Start ngrok tunnel for SSH (with retry logic)
        run: |
          nohup ngrok tcp 2222 --log=stdout > ngrok.log 2>&1 &
          echo "⏳ Waiting for ngrok to initialize..."
          for i in {1..10}; do
            sleep 3
            addr=$(grep -o 'tcp://[^\"]*' ngrok.log)
            if [[ ! -z "$addr" ]]; then
              echo "🎯 Tunnel address: $addr"
              break
            fi
          done
          if [[ -z "$addr" ]]; then
            echo "❌ Still no tunnel found. Here's the log output for debugging:"
            tail -n 20 ngrok.log
            exit 1
          fi

      - name: Create working directory
        run: mkdir -p ~/devbox

      - name: Start periodic artifact backups
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "📦 Starting periodic artifact backup loop..."
          while true; do
            tar -czf devbox.tar.gz -C ~/ devbox
            timestamp=$(date +%s)
            cp devbox.tar.gz devbox-$timestamp.tar.gz
            gh run upload-artifact --name "devbox-$timestamp" devbox-$timestamp.tar.gz || echo "⚠️ Upload failed"
            sleep 60
          done
